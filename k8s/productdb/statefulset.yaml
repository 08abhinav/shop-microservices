apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: product-db
  namespace: dev
  labels:
    app: mongo
spec:
  serviceName: mongo-product-svc
  replicas: 1
  selector:
    matchLabels:
      role: product-db
  template:
    metadata:
      labels:
        role: product-db
    spec:
      containers:
        - name: product-mongodb
          image: mongo:latest
          ports:
            - containerPort: 27017
          envFrom:
            - configMapRef:
                name: product-db-config
            - secretRef:
                name: product-db-secret
          volumeMounts:
            - name: product-mongo-persistent-storage
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: product-mongo-persistent-storage
      spec:
        storageClassName: product-mongodb-storage
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi