apiVersion: v1
kind: Service
metadata:
  name: mongo-user-svc
  namespace: dev
  labels:
    name: mongo-user-svc
spec:
  clusterIP: None
  selector:
    role: user-db
  ports:
    - port: 27017
      targetPort: 27017