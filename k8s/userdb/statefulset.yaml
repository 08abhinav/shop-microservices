apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db
  namespace: dev
  labels:
    app: mongo
spec:
  serviceName: mongo-user-svc
  replicas: 1
  selector:
    matchLabels:
      role: user-db
  template:
    metadata:
      labels:
        role: user-db
    spec:
      containers:
        - name: user-mongodb
          image: mongo:latest
          ports:
            - containerPort: 27017
          envFrom:
            - configMapRef:
                name: user-db-config
            - secretRef:
                name: user-db-secret
          volumeMounts:
            - name: user-mongo-persistent-storage
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: user-mongo-persistent-storage
      spec:
        storageClassName: user-mongodb-storage
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi